#include <iostream>
#include <ctime>

using namespace std;

void task1()
{
    count << "\ntask1\n";

    char* c = new char;
    bool* b = new bool;
    short* s = new short;
    int* i = new int;
    long* l = new long;
    float* f = new float;
    double* d = new double;

    count << "Enter char:";
    cin >> *c;

    count << "Enter bool:";
    cin >> *b;

    count << "Enter short:";
    cin >> *s;

    count << "Enter int:";
    cin >> *i;

    count << "Enter long:";
    cin >> *l;

    count << "Enter float:";
    cin >> *f;

    count << "Enter double:";
    cin >> *d;

    count << "\nValues:\n";
    count << *c << endl;
    count << *b << endl;
    count << *s << endl;
    count << *i << endl;
    count << *l << endl;
    count << *f << endl;
    count << *d << endl;

    delete c;
    delete b;
    delete s;
    delete i;
    delete l;
    delete f;
    delete d;
}
